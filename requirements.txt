# ── Core / validation ────────────────────────────────────────────
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
python-dotenv>=1.0,<2

# ── LLM — local via Ollama (open-source; replaces OpenAI API) ────
ollama>=0.2,<1
langchain>=0.2,<0.4
langchain-community>=0.2,<0.4
langchain-ollama>=0.1,<0.3

# ── Agent framework ───────────────────────────────────────────────
langgraph>=0.2,<0.4

# ── Embeddings & reranker (local, sentence-transformers) ─────────
sentence-transformers>=3.0,<4
torch>=2.3                   # sentence-transformers runtime dep

# ── PDF parsing (open-source; PyMuPDF) ───────────────────────────
pymupdf>=1.24,<2

# ── Vector store — PostgreSQL + pgvector ─────────────────────────
psycopg2-binary>=2.9,<3
pgvector>=0.3,<1
sqlalchemy>=2.0,<3

# ── API ───────────────────────────────────────────────────────────
fastapi>=0.111,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1

# ── Eval (open-source; replaces hosted eval APIs) ────────────────
ragas>=0.1,<0.3
datasets>=2.19                # ragas dependency

# ── Tests ────────────────────────────────────────────────────────
pytest>=8.2,<9
pytest-asyncio>=0.23,<1
pytest-cov>=5.0,<6
